# Context

## Acceptance showcase

The FeatureShowcase console also renders a bundled RDLC definition into a separate seven-file directory: source RDLC, page PNG, PDF, HTML, XLSX, DOCX, and an engine feature manifest.

## Architecture

The legacy solution contains a decompiled ReportViewer engine. Its headless and chart paths still use System.Drawing/GDI/Uniscribe, while WinForms is Windows-only. A v2 migration has started with a backend-neutral rendering boundary in `ReportViewerCore.Rendering.Abstractions` and a SkiaSharp/HarfBuzz implementation in `ReportViewerCore.Rendering.Skia`. The net10 WinForms compatibility assembly now contains the Windows-only `RplPortableDocumentAdapter`, which snapshots the internal v1 RPL page model into `IReportPageSource`; the opt-in WinForms portable bridge falls back to this adapter only when the constrained RDLC engine rejects the report.

## Modules

The new rendering slice exposes geometry, font, text shaping, image codec/resolver, subreport resolver, chart, canvas, report document, and renderer contracts without System.Drawing types. `ReportViewerCore.Engine` parses constrained multi-tablix/subreport/page-header-footer/textbox-hyperlink/nested-item/styled-text and mixed textbox/image/rectangle/line/bar/column/line/area/pie chart report items, keeps other body items alongside tablixes, applies tablix sorting, resolves dataset names case-insensitively, traverses nested `TablixMember` group expressions in document order, partitions composite group scopes, renders nested group headers and group-footer templates, honors constrained grouped page breaks, and evaluates `CountRows`, `Count`, `First`, `Last`, `Sum`, `Avg`, `Min`, and `Max` during pagination, applies allow-listed conditional visibility to standalone items and tablix-cell text/images, preserves initial `Hidden` state for static `ToggleItem` reports, applies parent offsets, binds enumerable rows into pages, maps text color/writing mode, applies RDLC parameter defaults, resolves allow-listed field/parameter expressions including concatenation, `IIF`, comparisons, `Format`, and pure string functions in body and tablix cells, maps explicit parent parameters into child subreports, and exposes `IReportPageSource`/`ReportPageSourceAdapter` for paginated legacy-to-portable bridging. `ReportViewerCore.Headless.LocalReport` owns the local workflow and `ServerReport` delegates remote rendering to an explicit transport; `HttpReportServerTransport` uses URL access and `IReportServerAuthenticator` for explicit credentials. SkiaSharp handles PNG/PDF on every RID, `ReportViewerCore.Rendering.Html` handles SVG-backed HTML, and `ReportViewerCore.Rendering.OpenXml` handles text/hyperlink/PNG-image XLSX/DOCX baselines, worksheet dimensions, report page-sized DOCX sections, page-relative floating image/chart anchors, rectangle/line shape parts, and native bar/column/line/area/pie chart parts. `ReportViewerCore.Rendering.Windows` is a display-only WinForms adapter over the same Skia pages, exposed from the legacy assembly as net10-only `Microsoft.Reporting.WinForms.PortableReportViewer`. Both legacy `Microsoft.Reporting.NETCore.LocalReport` and `Microsoft.Reporting.WinForms.LocalReport` expose net10-only `CreatePortableDocument`/`RenderPortable` bridges; legacy `Render` remains unchanged. Existing legacy controls remain available for compatibility.

## Data Models

## Dependencies

The new backend pins SkiaSharp 2.88.9 and HarfBuzzSharp 8.3.1.1, including Linux native assets. The legacy compatibility projects directly pin `System.Security.Cryptography.Xml` 10.0.10 to override the vulnerable transitive floor from the WCF dependency chain; the package metadata mirrors that pin for net8/net9/net10. The seven v2 projects now package a migration README and symbol archive with repository metadata. The final v2 package must validate native assets for `osx-arm64`, `osx-x64`, `linux-x64`, `linux-arm64`, and `win-x64`.

## Runtime Behavior

## Test Strategy

`tests/ReportViewerCore.Rendering.Tests` currently covers bitmap rendering, Latin/Thai/Arabic/RTL/CJK/vertical text shaping, PDF routing, semantic HTML/security behavior, OpenXML package structure, RDLC field binding/pagination, fixture-driven mixed bar/column/line/area/pie charts, fixture-driven international text styles/directions, and the LocalReport/ServerReport facades. `ReportViewerCore.Sample.FeatureShowcase` is the executable two-page acceptance example; it exports PNG pages, PDF, HTML, XLSX, DOCX, and a feature manifest after each local/CI run. The reusable cross-format gate validates both the direct-canvas and RDLC showcase scopes for semantic markers, page counts, XML/SVG structure, and byte-identical embedded Office previews. GitHub Actions validates the portable tests and sample publish for `linux-x64`, `linux-arm64`, `osx-arm64`, `osx-x64`, and `win-x64`, plus Windows-targeted adapter/legacy bridge builds and smoke samples; local validation has published and executed `osx-arm64` and `win-x64`, while matching hosted runners remain the final independent confirmation.
The chart fixture now also covers Doughnut, while Radar remains the explicit unsupported chart regression; all six supported chart kinds are asserted across HTML, PDF, XLSX, and DOCX paths.
The aggregate fixture now carries `Min`/`Max` content, and the OpenXML fixture test parses every emitted XML part to catch malformed package structure.
The test suite now also covers allow-listed `Len`/`Trim`/`UCase`/`LCase`/`InStr`/`Replace`/`Mid`, quoted-literal-safe comparisons, case-insensitive dataset/parameter lookup, RDLC textbox hyperlink relationships across HTML/PDF/XLSX/DOCX including relative targets, OpenXML internal relationship targets, worksheet dimensions, report-sized DOCX sections, per-page DOCX section sizes and next-page breaks, native Word multiline breaks, paginated Word/Excel outputs, page-relative DOCX image/chart anchors, all-edge page-clipped OpenXML images with source crop metadata, page-clipped OpenXML vector rectangles/lines with visible endpoint assertions, direction-aware page-clipped text/table-cell/hyperlink visible bounds and chart destinations with fully off-page objects and hyperlink relationships omitted, platform fallback per-glyph advances, Skia multiline text, invalid legacy page-source inputs, HTTP transport endpoint/format rejection, nested page decorations, mixed tablix/subreport composition, explicit unsupported nested subreport diagnostics, recursive tablix-cell visual offsets, shared page-size/delegate validation, finite chart values, network-path hyperlink rejection, finite font/page-size validation, vertical PDF hyperlink annotations, terminal sibling row-group branches, nested sibling branches with scoped subtotals and a root total, sibling branch `Start`/`End`/`StartAndEnd` page breaks, nested child `End` breaks before static subtotals, nested child `StartAndEnd` breaks after static wrapper subtotals, dynamic/static/dynamic wrapper children, single-root nested sibling branches with and without a static header, leading static child rows, and root interstitial static rows alongside trailing totals.
Shareable VSTest results belong in `artifacts/test-results/`; cross-platform smoke outputs belong in `artifacts/cross-platform/`, while build/test binaries remain generated under each project's `bin/<Configuration>/net10.0/` folder.
The reusable `.devbuddy/tools/validate_v2_artifacts.py` tool also validates TRX outcomes and verifies that every source RDLC fixture is non-empty, well-formed, and copied into the test output directory. The paired cross-format tool validates both showcase scopes for semantic/page-preview parity plus expected Office hyperlink, image, chart, and shape parts.
The fixture coverage index lives in `tests/fixtures/README.md`; source and test-output RDLC filenames must match exactly, and the current set contains 69 fixtures. The empty-tablix `NoRowsMessage` path is covered by `no-rows-message.rdlc` across HTML/PDF/XLSX/DOCX, pure search/replace functions by `string-functions-advanced.rdlc`, culture-aware numeric formatting by `format-number.rdlc`, native HTML/Word multiline breaks by `multiline.rdlc`, nested page decorations by `nested-header-footer.rdlc`, mixed top-level tablix/subreport content by `mixed-tablix-subreport.rdlc`, terminal and nested sibling row-group branches by `sibling-group-branches.rdlc`, `sibling-group-no-header.rdlc`, `sibling-group-start-pagebreak.rdlc`, `sibling-group-start-end-pagebreak.rdlc`, `sibling-group-start-end-pagebreak-disabled.rdlc`, `nested-sibling-group-branches.rdlc`, `nested-sibling-child-end-pagebreak.rdlc`, `nested-sibling-single-root.rdlc`, and `nested-sibling-single-root-no-header.rdlc`, hierarchy-first arbitrary member-tree traversal by `arbitrary-nested-member-tree.rdlc`, linear nested `StartAndEnd` page breaks by `nested-group-start-end-pagebreak.rdlc`, nested static detail/subtotal rows by `nested-group-static-detail-subtotal.rdlc`, `nested-static-detail-subtotal-sibling-branch.rdlc`, and `nested-static-detail-subtotal-sibling-branch-no-header.rdlc`, composite nested static-detail/subtotal sibling page breaks by `nested-static-detail-subtotal-sibling-branch-end-pagebreak.rdlc`, `nested-static-detail-subtotal-sibling-branch-start-end-pagebreak.rdlc`, and parameter-disabled `nested-static-detail-subtotal-sibling-branch-start-end-pagebreak-disabled.rdlc`, shape-matched single-child and multi-child static wrappers by `nested-static-wrapper-single-child.rdlc` and `nested-static-wrapper-multiple-children.rdlc`, constrained OpenXML merged cells by `merged-cell-table.rdlc`, page-clipped embedded image crop/anchor parity by `image-openxml-clipped.rdlc`, parameter-disabled group page breaks by `grouped-pagebreak-disabled.rdlc`, field-disabled group page breaks by `grouped-pagebreak-field-disabled.rdlc`, explicit unsupported nested subreports by the two `unsupported-*-subreport.rdlc` fixtures, and ambiguous top-level static sibling boundaries by `unsupported-static-sibling-boundary.rdlc` and `unsupported-static-leading-sibling-boundary.rdlc`.
The parameter-disabled page-break regression verifies enabled and disabled documents through the PNG, HTML, PDF, DOCX, and XLSX renderers, not only through `ReportDocument.Pages`.

Font policy is explicit: Skia uses platform family matching for ordinary requests, fails closed for unknown families, and accepts only existing/loadable caller-registered files. OpenXML carries the requested family/style metadata and does not embed font bytes implicitly; matching-RID fallback remains an environment validation concern.

## DevBuddy

Resolved memory root: `C:\Codes\reportviewercore\.devbuddy`

## Next-session handoff

DOCX/XLSX now place a page preview rasterized from the same Skia canvas as the PNG above their native Office semantic objects. This is the visible Office layout contract: preview image bytes must equal each corresponding PNG page, while hidden/off-viewport text, image, shape, hyperlink, and chart parts preserve OpenXML semantics and editability metadata.

Use [`docs/cross-platform-v2-checklist.md`](../docs/cross-platform-v2-checklist.md) as the source of truth for completed work, remaining phases, verification commands, and Windows-only validation. The RPL/SPB adapter is compile-verified for net10 WinForms, and both legacy bridge samples now execute locally on Windows with semantic/page-count comparisons; the hosted `windows-latest` job remains the final independent confirmation. The next implementation priority is deeper RDLC member parity, followed by advanced renderer contracts.

The local `ReportViewerCore.Sample.WinForms.V2` Release executable was also opened under Windows UI automation: its `PortableReportViewer` displayed the Alpha/Beta table without a startup exception, then closed cleanly. Hosted `windows-latest` remains the independent CI confirmation.
